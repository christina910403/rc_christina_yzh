# NotifyHub 统一外部通知服务产品需求文档

> 文档状态：V1.1（事件路由 MVP 基线）
> 更新日期：2026-07-15
> 适用范围：NotifyHub 首个可用版本（MVP）

## 1. 文档概述

### 1.1 文档目的

本文档定义 NotifyHub 首版的产品目标、使用角色、功能范围、关键业务规则、可靠性要求、安全边界和验收标准，作为产品评审、技术设计、研发、测试和上线验收的共同依据。

本文档描述系统“必须提供什么能力以及外部可观察行为”，不指定具体编程语言、数据库、消息队列或调度框架。具体技术选型由后续技术设计阶段完成。

### 1.2 产品定义

NotifyHub 是供企业内部业务系统使用的统一外部 HTTP 通知服务。它接收内部系统提交的通知任务，将任务可靠持久化，并异步投递至广告、CRM、库存等外部供应商提供的 HTTP(S) API。

NotifyHub 的核心价值是将“业务事件处理”和“外部通知可靠投递”解耦，使业务系统不需要自行处理供应商短暂不可用、网络异常、超时、重试、死信和投递审计等通用问题。

## 2. 背景与问题

企业内部多个业务系统会在关键事件发生后调用外部供应商 API，例如：

- 用户通过第三方广告系统引流并完成注册后，通知对应广告系统。
- 用户成功订阅服务后，通知 CRM 系统更新 Contact 状态。
- 用户购买商品后，通知库存系统执行库存变更。

当前场景存在以下共性问题：

- 不同供应商的请求地址、Header、Body 和鉴权方式不同。
- 外部服务可能出现网络抖动、响应超时、限流、服务故障或长时间不可用。
- 若各业务系统分别实现重试、审计和告警，容易形成重复建设与行为不一致。
- 业务系统只需要确认通知任务已被可靠受理，不需要同步等待或解析外部 API 的响应内容。

## 3. 产品目标与成功指标

### 3.1 产品目标

1. 为内部业务系统提供统一、稳定的通知任务受理入口。
2. 在任务受理成功后，以“至少一次”语义尽可能可靠地投递至目标地址。
3. 统一管理失败重试、死信、人工重放、告警和投递审计。
4. 支持不同事件调用不同 API，以及同一事件向多个 API 独立扇出。
5. 防止通知能力被用于访问未授权地址或泄露敏感信息。

### 3.2 首版成功指标

| 指标 | 首版目标 |
| --- | --- |
| 受理接口可用性 | 月度不低于 99.9% |
| 受理可靠性 | 返回“已受理”的任务必须已完成可靠持久化 |
| 首次投递及时性 | 正常无积压时，受理后 30 秒内开始首次投递 |
| 处理容量 | 常态 10 QPS，峰值 50 QPS |
| 积压能力 | 支持最多 10 万条待处理任务 |
| 可追溯性 | 任务从受理到成功、死信或重放的全过程可查询 |
| 数据保留 | 任务及投递记录默认保留 30 天 |

投递最终成功率受外部供应商可用性、请求内容正确性和目标配置影响，不作为 NotifyHub 单方面的服务承诺；NotifyHub 对可重试失败承担按既定策略执行重试且不静默丢失任务的责任。

## 4. 用户角色

| 角色 | 主要诉求 | 首版能力 |
| --- | --- | --- |
| 业务系统开发者 | 简单提交通知，不在业务链路中同步等待供应商响应 | 提交、幂等受理、查询任务状态 |
| 平台配置管理员 | 维护稳定供应商目标和安全白名单 | 通过受控配置文件维护目标与白名单 |
| 运维人员 | 发现故障、定位失败、处理死信 | 查看状态与审计记录、接收告警、人工重放 |
| 安全/审计人员 | 确认访问边界与敏感数据保护 | 查询脱敏后的目标、请求摘要和操作记录 |

首版不提供可视化管理后台。配置管理员和运维人员通过配置发布流程及内部运维接口完成操作。

## 5. 核心使用场景

### 5.1 标准事件路由

业务系统提交版本化标准事件，包括事件 ID、事件类型、发生时间、可选 Routing Key 和 Payload，不直接指定外部 URL、Header 或 Body。NotifyHub 根据事件类型、Routing Key 和生效窗口匹配路由，并将标准事件转换为目标 API 所需请求。

典型场景：注册事件按广告渠道回传不同广告平台；订阅成功事件更新 CRM；订单支付事件同时通知库存和 CRM。

### 5.2 多 API 独立扇出

一个事件可以匹配零条、一条或多条路由。NotifyHub 在同一数据库事务中保存事件和全部投递任务，每条投递独立执行、重试和进入死信；一个目标失败不得阻塞其他健康目标。

### 5.3 失败恢复

当外部目标发生网络异常、超时、限流或服务端错误时，NotifyHub 自动重试。任务达到最大重试次数后进入死信并触发告警。运维人员完成问题排查或目标恢复后，可手工重放死信任务。

## 6. 产品范围与系统边界

### 6.1 首版范围

- 统一受理内部版本化标准事件。
- 事件 Schema 校验、路由匹配和多 API 原子扇出。
- 预注册外部系统、API 操作和受限 Header/Query/Body 模板。
- 业务事件维度的幂等受理。
- 异步投递、自动重试、死信和人工重放。
- 任务状态查询和投递尝试记录查询。
- HTTPS 白名单、内网地址拦截、敏感信息保护。
- 基础容量、延迟、积压、失败率和死信告警。

### 6.2 不在首版范围

- 不负责业务事件编排、审批、定时营销或跨系统工作流。
- 不执行任意脚本或通用工作流，只提供受限声明式字段映射。
- 不解析或使用外部 API 的响应 Body 驱动业务状态。
- 不保证严格“仅一次”投递；供应商或网络异常可能导致重复请求。
- 不提供可视化管理后台。
- 不提供面向外部客户的开放平台能力。
- 不支持非 HTTP(S) 协议或调用方临时提交任意 URL。
- 不替代业务系统自身的数据库事务或本地事件可靠发布机制。

### 6.3 上下游责任边界

业务系统负责：

- 在业务事件具备提交条件后调用 NotifyHub。
- 提供稳定且唯一的业务事件 ID。
- 按约定的事件版本和 JSON Schema 提交规范 Payload。
- 确保重复外部通知不会造成不可接受的业务后果，必要时与供应商约定幂等键。
- 对永久性参数错误修正数据后再重新提交。

NotifyHub 负责：

- 对事件进行 Schema 校验、幂等判断、路由匹配和可靠持久化。
- 根据受限模板生成不同 API 所需的 Header、Query 和 Body。
- 在已持久化后返回受理结果，并异步执行投递。
- 按规则识别成功、可重试失败和不可重试失败。
- 记录每次投递尝试，执行重试、死信、告警和人工重放。
- 保护目标地址、鉴权信息和日志中的敏感数据。

外部供应商负责：

- 提供可访问的 HTTPS API 和明确的请求规范。
- 正确处理 NotifyHub 发送的请求，并自行保证其业务操作的幂等性。
- 在限流时尽可能通过 HTTP 429 和 `Retry-After` 表达恢复建议。

## 7. 核心业务流程

### 7.1 正常流程

1. 业务系统生成全局稳定的业务事件 ID，并提交版本化标准事件。
2. NotifyHub 完成身份认证、事件权限、时间和 JSON Schema 校验。
3. NotifyHub 按“来源系统 + 业务事件 ID”执行幂等判断。
4. NotifyHub 根据事件类型、Routing Key 和生效窗口匹配全部有效路由。
5. NotifyHub 渲染每个 API 的 Header、Query 和 Body，在同一事务中持久化事件和全部投递任务。
6. NotifyHub 向业务系统返回“已受理”；业务系统无需等待供应商响应。
7. NotifyHub 异步发起首次外部请求。
8. 外部服务返回任意 2xx 后，任务标记为投递成功；响应 Body 不解析、不回传给业务系统。
9. 任务状态和投递尝试记录保留 30 天，供查询和审计。

### 7.2 重复提交

1. 业务系统因自身超时或重试再次提交相同任务。
2. NotifyHub 命中相同的幂等范围后，不创建新任务。
3. NotifyHub 返回首次创建的通知任务 ID 和当前状态。
4. 若原任务已经进入死信，重复提交不会自动绕过死信策略；应由运维人员重放，或由业务系统在修正内容后使用新的业务事件 ID 提交新任务。

### 7.3 失败与重试

1. 首次投递失败后，NotifyHub 根据 HTTP 状态码或网络错误判断是否可重试。
2. 可重试失败按既定时间表调度下一次尝试。
3. 任意一次重试收到 2xx 后立即停止后续重试并标记成功。
4. 不可重试失败或重试耗尽后进入死信并触发告警。
5. 运维人员处理原因后，通过运维接口手工重放。

### 7.4 人工重放

1. 运维人员根据告警和投递记录确认失败原因已消除。
2. 运维人员提交单条或受控批量重放请求，并填写操作原因。
3. NotifyHub 保留原通知任务 ID，创建新的投递轮次并重新执行首次投递。
4. 重放仍遵循相同的目标安全校验、成功判断和重试策略。
5. 系统记录操作者、时间、原因、重放范围和结果。

## 8. 功能需求

### 8.1 任务受理

| 编号 | 需求 |
| --- | --- |
| FR-001 | 系统必须仅允许经过认证和授权的内部调用方提交任务。 |
| FR-002 | 每个请求必须包含业务事件 ID、版本化事件类型、发生时间和 Payload；来源系统由认证凭证确定。 |
| FR-003 | 事件 Payload 及渲染后的单个请求 Body 均不得超过 1 MB。 |
| FR-004 | 系统必须先完成任务可靠持久化，再返回“已受理”。 |
| FR-005 | 校验失败或持久化失败时不得返回“已受理”，且不得产生可投递任务。 |
| FR-006 | 受理成功后返回通知任务 ID、受理状态、是否为重复请求及查询所需信息。 |
| FR-007 | 业务系统提交的通知任务不得同步等待外部 API 返回。 |

### 8.2 幂等受理

事件幂等键由以下两个维度共同确定：

1. 来源系统标识；
2. 业务事件 ID。

每条扇出任务另使用“内部事件主键 + 路由 ID”唯一约束，避免相同路由重复创建任务。

| 编号 | 需求 |
| --- | --- |
| FR-010 | 相同幂等键的重复提交不得创建新的通知任务。 |
| FR-011 | 重复提交必须返回原任务 ID 和当前状态。 |
| FR-012 | 一个事件匹配多条路由时，必须原子创建多条独立投递任务。 |
| FR-013 | 幂等记录至少覆盖任务及投递记录的 30 天保留期。 |
| FR-014 | 幂等受理只避免调用方重复提交产生多个任务，不等同于外部“仅一次”投递。 |

### 8.3 事件、API 与路由配置

受控配置至少包含：

- `EventDefinition`：事件 ID、版本、来源系统和 JSON Schema；
- `ExternalSystem`：外部系统 Base URL、并发、限流和负责人；
- `APIOperation`：具体 HTTP Method、Path、静态 Header、密钥引用、超时和重试策略；
- `EventRoute`：事件到 API 操作的映射、Routing Key、生效窗口和请求模板。

配置发生变更只影响变更后新受理的事件或显式选择当前配置的人工重放。历史投递必须保留路由版本、API 操作版本和实际请求快照。

### 8.4 请求映射

| 编号 | 需求 |
| --- | --- |
| FR-020 | 路由可独立定义动态 Header、Query 和 Body 模板。 |
| FR-021 | 动态 Header 只能使用 API 操作声明的白名单，不能覆盖传输 Header、鉴权 Header或幂等 Header。 |
| FR-022 | `Authorization`、`Cookie`、API Key 等敏感 Header 必须通过受管密钥引用注入。 |
| FR-023 | 模板必须使用严格缺失字段检查，只开放受审核的纯函数，禁止文件、网络、环境变量和任意代码执行。 |
| FR-024 | JSON Body 渲染后必须再次解析为合法 JSON；V1 同时支持 URL-encoded Form 编码器。 |
| FR-025 | 单条路由配置或渲染失败不得阻塞同一事件的其他健康路由，并必须进入可查询死信和告警。 |

### 8.5 异步投递

| 编号 | 需求 |
| --- | --- |
| FR-030 | 受理成功的任务必须异步进入待投递状态。 |
| FR-031 | 正常无积压时，系统必须在受理后 30 秒内开始首次投递。 |
| FR-032 | 外部 API 返回任意 2xx 时，系统必须将本轮任务标记为成功。 |
| FR-033 | 系统不得以响应 Body 内容判断业务成功或失败。 |
| FR-034 | 系统必须为每次尝试记录开始时间、耗时、结果分类、HTTP 状态码、错误摘要和下次计划时间。 |
| FR-035 | 日志和查询结果不得展示完整密钥、敏感 Header 或超出审计需要的请求 Body。 |

### 8.6 重试策略

首次投递失败后，默认最多再执行 6 次重试，总计最多 7 次投递尝试。重试间隔从上一次失败后计算：

| 重试序号 | 默认等待时间 |
| --- | --- |
| 第 1 次重试 | 1 分钟 |
| 第 2 次重试 | 5 分钟 |
| 第 3 次重试 | 30 分钟 |
| 第 4 次重试 | 2 小时 |
| 第 5 次重试 | 6 小时 |
| 第 6 次重试 | 24 小时 |

若供应商返回 HTTP 429 或 503 且包含合法的 `Retry-After`，系统可采用该建议时间，但不得早于平台安全下限或晚于平台允许的最大等待时间。

失败分类如下：

| 结果 | 分类 | 系统行为 |
| --- | --- | --- |
| 任意 2xx | 成功 | 标记成功并停止重试 |
| DNS 临时失败、连接失败、连接重置、响应超时 | 可重试 | 按策略重试 |
| 408、429、5xx | 可重试 | 按策略重试 |
| 其他 4xx | 不可重试 | 直接进入死信 |
| 3xx | 不可自动跟随 | 默认直接进入死信 |
| 安全校验失败、域名不在白名单、解析到私网地址 | 安全拒绝 | 不发起请求并进入死信，同时记录安全事件 |
| 请求组装失败或配置无效 | 配置错误 | 不发起请求并进入死信，同时告警 |

即使外部服务已经完成处理，NotifyHub 也可能因为响应在网络中丢失而执行重试。因此，至少一次语义允许重复投递，不能承诺严格仅一次。

### 8.7 死信与重放

| 编号 | 需求 |
| --- | --- |
| FR-040 | 不可重试失败和重试耗尽的任务必须进入死信状态，不得静默删除。 |
| FR-041 | 死信必须记录失败分类、最后一次错误、尝试次数和建议排查信息。 |
| FR-042 | 新增死信必须触发告警；同一目标大量失败时应聚合告警，避免告警风暴。 |
| FR-043 | 运维接口必须支持按任务 ID 重放，并支持设有数量上限的批量重放。 |
| FR-044 | 每次重放必须记录操作者和原因，并纳入审计。 |
| FR-045 | 重放不得修改原始业务事件 ID，不得清除历史投递记录。 |
| FR-046 | 重放前必须重新执行当前目标和安全策略校验。 |

### 8.8 状态查询

业务系统和运维人员可按权限查询事件和投递。首版至少支持按内部事件 ID、投递任务 ID 查询；运维场景还应支持按来源系统、业务事件 ID、路由、目标、状态和时间范围筛选。

任务状态定义如下：

| 状态 | 含义 | 是否终态 |
| --- | --- | --- |
| `ACCEPTED` | 已持久化，等待首次投递 | 否 |
| `DELIVERING` | 正在执行一次投递尝试 | 否 |
| `RETRY_WAIT` | 本次失败，等待下次重试 | 否 |
| `SUCCEEDED` | 已收到外部 API 的 2xx | 是 |
| `DEAD_LETTER` | 不可重试或重试耗尽 | 是，可人工重放 |

重放会为原任务创建新的投递轮次，任务查询结果必须同时展示历史轮次和当前轮次，避免覆盖历史失败事实。

## 9. 接口产品契约

本节只定义首版必须暴露的能力和行为，不约束最终 URL 命名及具体 JSON 字段命名。

### 9.1 业务接口

| 能力 | 必要输入 | 主要输出 |
| --- | --- | --- |
| 提交标准事件 | 业务事件 ID、事件类型、发生时间、Routing Key、Payload | 内部事件 ID、匹配路由、投递任务和重复标识 |
| 查询事件 | 内部事件 ID | 聚合状态、匹配路由和各投递状态 |
| 查询投递记录 | 投递任务 ID | 各投递轮次及尝试记录、脱敏错误摘要 |

### 9.2 运维接口

| 能力 | 权限与审计要求 |
| --- | --- |
| 查询死信 | 仅运维及审计角色可用，支持条件筛选 |
| 重放单条死信 | 记录操作者、原因、时间和结果 |
| 批量重放死信 | 必须限制单次数量，记录筛选条件和影响范围 |
| 查询服务健康与积压 | 展示受理、投递、重试、死信和目标健康摘要 |

### 9.3 受理响应语义

- “已受理”表示任务已可靠持久化并由 NotifyHub 接管后续投递，不表示外部供应商已成功处理。
- “重复请求”表示命中已有幂等任务，调用方应使用返回的原任务 ID 查询进度。
- “请求被拒绝”表示参数、权限或安全校验失败，NotifyHub 未承诺后续投递。
- “服务内部错误”表示未能完成可靠受理，调用方可使用相同业务事件 ID 安全重试。

## 10. 安全与合规要求

### 10.1 目标访问控制

- 生产环境仅允许配置中预注册的 HTTPS 目标，不接受调用方提交任意 URL。
- 白名单必须精确到允许的域名及端口；默认不接受通配范围过大的域名规则。
- 禁止 URL 中携带用户名或密码。
- 禁止访问回环、链路本地、私网、组播、保留地址及云环境元数据地址。
- 发起连接前必须复核 DNS 解析结果；解析结果变化时仍需满足地址限制，防止 DNS 重绑定。
- 默认不跟随 HTTP 重定向。未来若允许跟随，每一跳都必须重新执行白名单和地址校验。

### 10.2 密钥与敏感数据

- 供应商密钥必须存储于受管密钥系统，并通过引用方式注入请求。
- 配置文件、任务记录、普通日志和错误信息不得保存或输出明文密钥。
- `Authorization`、`Cookie`、API Key 等敏感 Header 必须全量脱敏。
- 请求 Body 默认不写入应用日志；问题排查使用内容摘要、长度和哈希等非敏感信息。
- 查询和重放操作必须经过身份认证、最小权限控制和审计。

### 10.3 输入限制

- 请求 Body 上限为 1 MB。
- Header 总大小、单个 Header 长度和 Header 数量必须设置平台上限。
- 拒绝换行注入、非法 Header 名称、平台保留 Header 和不受支持的 HTTP 方法。
- 每个来源系统和目标必须具备独立限流能力，防止单个调用方拖垮服务或供应商。

## 11. 可观测性与告警

### 11.1 关键指标

- 受理请求量、受理成功率、受理延迟和幂等命中量。
- 当前待投递、重试等待、投递中和死信任务数。
- 首次投递延迟、端到端投递延迟和各目标成功率。
- 按目标及错误分类统计的超时、429、4xx、5xx 和安全拒绝数量。
- 重试次数、重试成功率、死信新增量和人工重放结果。
- 最老待处理任务年龄和任务积压增长速度。

### 11.2 告警场景

- 受理接口可用性或错误率超过阈值。
- 首次投递延迟超过 30 秒目标且持续增长。
- 总积压或最老任务年龄超过阈值。
- 某目标连续失败、429 或 5xx 比例显著升高。
- 新增死信或死信短时间大量增长。
- 发生白名单拒绝、私网地址解析或其他安全事件。
- 持久化、调度或投递执行组件不可用。

告警信息不得包含密钥、完整敏感 Header 或完整请求 Body。

## 12. 非功能需求

### 12.1 性能与容量

- 常态处理能力为 10 QPS，峰值 50 QPS。
- 在正常依赖可用且无积压时，任务受理后 30 秒内开始首次投递。
- 服务必须能够保存并逐步消化 10 万条待处理任务，不因短时供应商故障丢失已受理任务。
- 单个目标限流不得阻塞其他健康目标的正常投递。

### 12.2 可用性与恢复

- 受理接口月度目标可用性不低于 99.9%。
- 服务进程重启、投递执行组件重启或短时基础设施故障后，已受理但未完成的任务必须能够继续处理。
- 任务被执行组件领取后、状态确认前发生故障时，系统应允许任务重新投递，以任务不丢失优先。
- 应支持根据持久化记录恢复任务状态，不依赖单一进程内存。

### 12.3 数据保留与审计

- 任务、投递尝试、死信、重放和配置版本摘要默认保留 30 天。
- 超过保留期的数据按企业数据治理要求归档或清理。
- 审计记录至少包含操作者/调用方、动作、对象、时间、结果和必要的脱敏摘要。

## 13. 验收标准

### 13.1 功能验收

1. 提交合法标准事件后，系统返回已受理，并按配置生成对应投递任务。
2. 不同事件可调用不同 API；同一事件可原子生成多条使用不同 Header 和 Body 的投递任务。
3. 相同来源和业务事件 ID 的重复提交返回同一内部事件，不重复创建投递任务。
4. 一个目标失败时，其他健康目标仍可独立完成投递。
5. 外部目标返回任意 2xx 时任务成功，响应 Body 不影响结果。
6. 网络超时、408、429 和 5xx 按默认时间表重试，某次成功后停止后续重试。
7. 普通 4xx、3xx、安全拒绝和无效配置不执行无意义重试，并进入死信。
8. 重试耗尽后任务进入死信、产生告警，且投递历史完整保留。
9. 运维人员可携带原因重放死信；重放操作可审计，且不覆盖原历史记录。
10. 任务状态和每次投递尝试可通过查询能力获取。

### 13.2 安全验收

1. 非白名单域名或端口被拒绝且不会发出网络请求。
2. 解析到回环、私网、链路本地或元数据地址的目标被拒绝。
3. 3xx 响应不会被自动跟随至未校验的新目标。
4. 明文长期密钥、非法 Header、超限 Body 和无效配置被拒绝。
5. 日志、查询结果、告警和审计信息中不存在明文密钥或敏感 Header。

### 13.3 容量与恢复验收

1. 在常态 10 QPS 和峰值 50 QPS 压力下，受理接口保持目标可用性且无已受理任务丢失。
2. 在积压 10 万条任务的场景下，服务可持续受理、查询并逐步消化积压。
3. 在投递执行组件重启后，未完成任务可恢复并继续投递。
4. 在供应商长时间不可用后，任务按策略重试并最终进入死信；供应商恢复后可人工重放成功。

## 14. 工程取舍与判断依据

### 14.1 选择至少一次，不选择严格仅一次

普通 HTTP 调用无法同时确认“供应商是否完成处理”和“成功响应是否可靠送达”。在响应丢失等场景下，为避免通知丢失，系统必须允许重试，因此首版选择至少一次语义。严格仅一次需要供应商共同支持幂等协议或事务协调，不属于单一通知服务可独立保证的能力。

### 14.2 先持久化再返回受理

如果仅将任务放入内存或在持久化前返回成功，服务故障会导致业务系统认为任务已被接管但实际丢失。首版将“已可靠持久化”作为受理成功的必要条件，以较小的受理延迟换取明确的可靠性承诺。

### 14.3 有限重试加死信，不无限重试

参数错误、权限错误或已下线供应商不会因为无限重试自行恢复。无限重试会长期占用容量并加重供应商压力。首版通过错误分类、有限退避重试、死信、告警和人工重放，在可靠性与资源可控之间取得平衡。

### 14.4 只支持预注册目标

MVP 不接受调用方提交任意 URL，避免系统成为内部网络代理并扩大 SSRF、DNS 重绑定和密钥泄露风险。新增供应商通过受控配置发布。

### 14.5 使用受限模板，不使用任意脚本

不同供应商的 Header 和 Body 差异是核心需求，因此 V1 提供声明式模板；但模板只能做字段投影、转义、时间格式化和少量纯函数，不提供通用脚本、网络或文件访问，避免演变为不可治理的工作流引擎。

### 14.6 允许使用消息队列但不绑定具体产品

持久化消息队列、数据库任务表或其他等价基础设施均可能满足可靠存储、延迟调度和故障恢复要求。PRD 只约束“不丢已受理任务”和可观察行为；具体选型应在技术设计阶段结合团队运维经验、现有基础设施、延迟队列能力和成本评估后确定。

## 15. 后续演进方向

### 阶段 1：首版稳定运行

- 完成本文档定义的事件路由、多 API 扇出、请求映射、重试、死信、重放、安全和基础可观测性。
- 通过配置文件维护预注册目标和白名单。
- 收集真实吞吐、目标数量、失败类型和重放频率数据。

### 阶段 2：治理与效率提升

- 将目标配置从配置文件升级为带审批、版本和回滚能力的管理 API。
- 增加按供应商隔离的并发、限流、熔断和故障降级能力。
- 增加死信批量处置、配置校验和运营统计能力。
- 根据真实使用需求建设可视化运营后台。

### 阶段 3：规模化与高级能力

- 支持队列和投递执行组件水平扩展、按目标分区和更大规模积压。
- 支持多地域容灾及更精细的服务等级目标。
- 在供应商明确支持幂等键时，提供标准化幂等 Header 透传能力。
- 仅在业务需求明确且边界可控时，评估模板化报文或轻量转换能力。

## 16. 上线前置条件

- 完成业务接口、运维接口和目标配置格式的技术设计评审。
- 完成身份认证、权限模型、白名单和密钥管理方案评审。
- 完成故障恢复、积压 10 万条和峰值 50 QPS 的容量验证。
- 完成超时、重试、死信、重放和重复投递场景的演练。
- 建立受理失败、积压、目标异常、死信和安全拒绝告警。
- 明确首批接入业务、目标供应商、责任人及故障升级路径。

## 17. 术语表

| 术语 | 定义 |
| --- | --- |
| 通知任务 | 业务系统提交、由 NotifyHub 负责异步投递的一条工作记录 |
| 业务事件 ID | 业务系统为一次确定业务事实生成的稳定唯一标识 |
| 目标 | 接收通知的外部供应商 HTTPS API |
| 至少一次 | 系统优先保证不丢失任务，但一次任务可能被外部目标接收多次 |
| 幂等受理 | 重复提交相同事件和目标时不创建多个 NotifyHub 任务 |
| 投递尝试 | NotifyHub 针对一个任务发起的一次外部 HTTP 请求 |
| 投递轮次 | 首次受理或一次人工重放触发的一组投递尝试 |
| 死信 | 因不可重试错误或重试耗尽而停止自动处理的任务状态 |
| 重放 | 运维人员在保留历史记录的前提下，重新启动死信任务的投递流程 |
