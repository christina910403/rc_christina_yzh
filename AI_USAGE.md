# AI 使用说明

## 使用范围

本项目在需求澄清、架构设计、工程取舍、文档整理和 Go MVP 代码生成过程中使用了 OpenAI Codex。AI 的参与是显著的：它根据原始需求图片和后续对话生成并修订了 PRD、实施规划、代码骨架、配置示例和测试。最终提交前仍应由提交者负责代码复核、运行验证和对设计结论承担责任。

## AI 提供的关键帮助

1. 将原始需求拆分为可靠受理、事件路由、请求映射、异步投递、重试、死信、重放、安全和可观测性等模块。
2. 对“不关心外部 API 返回值”给出协议解释：业务系统不等待响应、NotifyHub 不解析响应 Body，但仍需要使用 HTTP 状态码判断是否获得接收确认。
3. 在需求方进一步强调失败必须重试、长期不可用必须有处理机制后，补充了网络错误与 `408/429/5xx` 分类、退避重试、`Retry-After`、重试耗尽、死信和人工重放方案。
4. 在需求方明确“不同事件调用不同 API，同一事件也可能调用多个 API”后，将需求具体化为“EventDefinition → EventRoute → APIOperation → ExternalSystem”四层模型。
5. 在需求方明确不同 API 使用不同 Header 和 Body 后，设计了 API 级固定/密钥 Header 与路由级动态 Header/Body 模板的职责分离。
6. 分析至少一次语义下的故障窗口、重复投递风险、事件受理幂等、路由任务幂等和供应商幂等键。
7. 以需求方给出的 MVP 容量约束——常态 10 QPS、峰值 50 QPS、最多 10 万条积压——为前提，比较 PostgreSQL 任务表、RabbitMQ 和 Kafka，并建议 MVP 使用 PostgreSQL。
8. 生成 Go MVP 的初始实现，包括 HTTP API、PostgreSQL 存储、Worker、模板、重试策略、示例配置和单元测试。

## 需求方提供的约束和关键纠正

以下内容来自原始需求或需求方在对话中的明确说明，不属于 AI 自行提出的贡献：

1. MVP 的容量目标是常态 10 QPS、峰值 50 QPS，并支持最多 10 万条待处理任务。AI 只是在这些约束下进行了中间件比较和选型分析。
2. 不同系统事件需要调用不同 API，并且不同 API 的 Header 和 Body 不同。需求方据此否定了仅由业务系统直接提交目标请求的简化设计。
3. “业务系统不关心外部 API 返回值”不能被解释为失败后不处理。AI 最初主要说明了 `2xx` 接收确认，需求方进一步追问失败、持续失败和 API 长期不可用时怎么办，推动方案明确加入完整重试、死信和恢复机制。
4. README 对系统的理解不能只描述解耦，还必须说明可靠持久化、幂等传递、至少一次语义以及不解析返回 Body 与保留重试机制之间的关系。

## AI 给出但没有采纳的建议

1. **最初建议由业务系统直接指定目标并提交供应商最终 Body**：后续需求明确指出不同系统事件需要自动选择不同 API，因此没有采纳，改为标准事件、路由订阅和请求模板。
2. **早期 AI 技术草稿中“收到任意 HTTP 响应即认为送达”**：没有采纳。需求方强调失败必须重试；500 只证明请求到达 HTTP 服务，不能证明库存或 CRM 请求被接受。最终使用 2xx 成功、408/429/5xx 重试。
3. **使用 Kafka 或 RabbitMQ**：没有采纳。当前吞吐和积压规模下 PostgreSQL 可以同时完成原子持久化、延迟调度、查询和死信，独立 MQ 会增加双写和运维复杂度。
4. **开放调用方提交任意 URL**：没有采纳。该能力会把系统变成内部网络代理并显著扩大 SSRF、DNS 重绑定和密钥泄露风险，MVP 仅允许预注册目标。
5. **通用脚本或工作流引擎**：没有采纳。MVP 只提供受限声明式模板，避免不可审计代码和系统边界膨胀。
6. **无限自动重试或首版自适应熔断**：没有采纳。最终使用有限退避、死信、告警和人工重放；自动熔断根据真实运行数据再评估。

## 最终关键决策及原因

以下决策是在需求方持续澄清和审查 AI 建议后确定的，而不是直接接受某一次 AI 输出：

1. **事件路由属于 MVP 核心能力**：需求方明确要求不同事件调用不同 API、不同 API 使用不同 Header 和 Body，因此不能只做目标透传。
2. **业务系统不解析外部响应，但 NotifyHub 必须判断 HTTP 状态并保留重试机制**：需求方明确指出“不关心返回值”不能取消失败重试，这是“调用方解耦”和“可靠投递”能够同时成立的必要条件。
3. **选择至少一次**：在请求已处理但响应丢失的场景，系统选择可能重复而不是可能漏送；供应商支持时使用事件 ID 去重。
4. **选择有限重试和持久化死信**：需求方要求明确失败和长期不可用时的处理；最终选择有限退避、死信、告警和重放，而不是失败即结束或无限重试。
5. **选择 PostgreSQL 任务表作为 MVP 队列**：这是基于需求方已经给定的 10～50 QPS 和 10 万积压约束做出的取舍，可避免数据库与消息队列双写；未来只有在真实瓶颈出现后才演进到 Outbox + MQ。
6. **自动重试固定使用不可变请求快照**：避免配置变化导致同一任务每次重试发送不同报文；人工重放若切换配置版本必须显式记录。

## 人工复核建议

在将此项目作为最终提交或部署前，提交者应重点复核：PostgreSQL 租约与并发边界、生产 HTTPS/出站策略、密钥管理接入、模板字段与真实供应商契约、重试上限、数据保留政策及集成压测结果。
